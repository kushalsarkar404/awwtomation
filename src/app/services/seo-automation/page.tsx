import { sharedMetadata } from "../_shared/metadata"

export const metadata = sharedMetadata["seo-automation"]

import SEOAutomationPage from "./UI"

export default function Page() {
  return <SEOAutomationPage />
}
