import { sharedMetadata } from "../_shared/metadata"

export const metadata = sharedMetadata["blog-automation"]

import BlogAutomationPage from "./UI"

export default function Page() {
  return <BlogAutomationPage />
}
